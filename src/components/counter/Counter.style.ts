import styled from '@emotion/styled';
import { CounterIcon } from '../../assets/icons/CounterIcon';

export const CounterIconWrapper = styled(CounterIcon)`
    width: 40px;
    background-color: var(--shade1);
    border-radius: 50%;
`;
